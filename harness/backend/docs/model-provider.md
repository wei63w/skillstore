# 代码模型供应商策略

Harness 自动生成可运行代码必须通过 `CodeModelProvider` 抽象调用代码/推理型大模型。

## 当前规则

- 支持多供应商注册，不允许执行器直接绑定单一供应商 SDK。
- 第一版使用 `stub` provider 验证结构化补丁计划、风险扫描和补丁应用流程。
- 真实供应商密钥必须通过环境变量或配置中心注入，禁止写入仓库。
- 多模态模型后置为可选视觉评审扩展，不阻塞代码生成闭环。
