spring:
  application:
    name: openclaw-skill-store-test
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:skillstore;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false
